<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="styles.css" rel="stylesheet" type="text/css">
    <title>Edit User</title>
    <style>
      /* styles.css */
        /* General body styling */
        body {
            background-color: #faefe0;
            text-align: center;
            padding: 20px;
        }

        /* Navigation bar */
        .navbar {
            position: fixed;
            top: 0;
            width: 100%;
            background-color: #faefe0;
            color: #f7bcba;
            padding: 15px;
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 1000; /* Ensure it stays above other content */
            flex-direction: column; /* Stack links and brand vertically */
        }

        .navbar-content {
            text-align: center;
        }

        .navbar-brand {
            font-size: 2.5em;
            font-weight: bold;
            font-family: Georgia, serif;
        }

        .navbar-links {
            list-style: none;
            display: flex;
            gap: 20px;
            background-color: #faefe0;
            padding: 10px 15px;
            transition: background-color 0.3s;
        }

        .navbar-links a {
            color: #fff;
            text-decoration: none;
            font-size: 1em;
            background-color: #f7bcba; 
            padding: 10px 20px; /* Button padding */
            border-radius: 8px; /* Rounded corners */
            transition: background-color 0.3s;
            display: inline-block;
            font-weight: bold; /* Make text bold for better readability */
        }

        .navbar-links a:hover {
            background-color: #f5aeac; /* Darker pink on hover */
        }

        /* Add padding to the top of the page content to avoid overlap */
        .content {
            padding-top: 140px; /* Adjust this based on the height of your navbar */
        }

        /* Dropdown details menu */
        details {
            font-size: 18px;
            font-weight: bold;
            margin: 10px 0;
            padding: 15px;
            cursor: pointer;
            background-color: #f4e4e4;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        /* Background color blocks */
        #even {
            background-color: #faefe0;
        }

        #odd {
            background-color: #f7bcba;
        }

        /* Image Gallery */
        .gallery {
            display: flex;
            justify-content: center;
            gap: 10px;
            flex-wrap: wrap;
            margin-top: 20px;
        }

        .gallery-img {
            width: 150px;
            height: auto;
            cursor: pointer;
            border-radius: 8px;
            transition: transform 0.3s;
        }

        .gallery-img:hover {
            transform: scale(1.1);
        }

        .modal {
            display: none;
            position: fixed;
            z-index: 1;
            padding-top: 100px;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.8);
        }

        .modal-content {
            margin: auto;
            display: block;
            max-width: 80%;
            max-height: 80%;
        }

        .close {
            position: absolute;
            top: 30px;
            right: 30px;
            color: white;
            font-size: 40px;
            font-weight: bold;
            cursor: pointer;
        }

        .close:hover {
            color: #bbb;
        }

        /* Specific Form Styling */
        .vh-100 {
            height: 100vh;
        }

        .container-fluid {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .form-container {
            width: 350px;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 8px;
            background-color: #f8f9fa;
        }

        .btn-custom {
            background-color: #ff69b4;
            color: white;
        }

        .btn-custom:hover {
            background-color: #ff85c0;
        }
    </style>
    </head>
    <body>
    <!-- Navigation Bar -->
      <nav class="navbar">
        <div class="navbar-brand">Haley Esthetics</div>    
        <ul class="navbar-links">
            <li><a href="/">Home</a></li>
            <li><a href="/policy">Policies</a></li>
            <li><a href="/pricing">Pricing</a></li>
            <li><a href="/reviews">Reviews</a></li>
            <li><a href="/admin">Admin Users</a></li>
            <li><a href="/admin_reviews">Admin Reviews</a></li>
        </ul>
      </nav>
      <br><br><br><br><br><br>

<!-- Admin Page Title -->
  <h1 style="text-align: center">Administration Page</h1>
  <h2 style="text-align: center;">Edit User</h2>
  <form action="/editUser/<%= user.email %>" method="POST">
    <!-- First Name -->
    <label for="first_name">First Name:</label>
    <input type="text" id="first_name" name="first_name" value="<%= user.first_name %>"><br>
    
    <!-- Last Name -->
    <label for="last_name">Last Name:</label>
    <input type="text" id="last_name" name="last_name" value="<%= user.last_name%>"><br>
    
    <!-- Phone Number -->
     <label for="phone">Phone:</label>
     <input type="text" id="phone" name="phone" value="<%= user.phone %>"><br>
    
     <!-- Submit Button -->
    <button type="submit">Update</button>
  </form>
</body>
</html>